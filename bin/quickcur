# opens source from clipboard in vim, then removes temp file
# and sticks back in clipboard
# sort of a poor man's quickcursor, which I highly recommend.
pbpaste > tmp.html
vim -c ":set filetype=html" -c ":%s///g" tmp.html
cat tmp.html | pbcopy
rm tmp.html
